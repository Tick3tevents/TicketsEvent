

export async function mintTicketNFT(publicKey, eventId) {
  // Тут можно будет интегрировать Metaplex для реального минта
  console.log(`Минтим NFT для пользователя ${publicKey.toBase58()} на событие ${eventId}`);
  // пока эмулируем успешное создание
}
